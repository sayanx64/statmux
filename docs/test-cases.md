# Test Cases & QA Validation Report
## Project: statmux — Unified Coding Analytics Engine

All test cases below were executed against the live running system (`http://localhost:3001` Express backend, `http://localhost:3000` Next.js frontend, Supabase PostgreSQL, Auth, and Storage) and genuine exported application modules.

---

| Test Case ID | Requirement ID | Test Description | Input / Preconditions | Expected Output | Actual Output | Result (Pass/Fail) |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **TC01** | **FR1** | Valid User Authentication | `email: "statmux@sayan.cyou"`, `password: "Password123!QA"` | HTTP 200 OK; JWT Bearer access token issued; session persisted. | `tokenIssued: true`, `userEmail: "statmux@sayan.cyou"`, `tokenType: "bearer"`. | **Pass** |
| **TC02** | **FR1** | Authentication with Invalid Password | `email: "statmux@sayan.cyou"`, `password: "WrongPassword999!"` | HTTP 400 Bad Request; error message returned; no session created. | `errorMessage: "Invalid login credentials"`, `sessionCreated: false`. | **Pass** |
| **TC03** | **FR1, NFR2** | Protected Route Access with Malformed/Expired JWT | `GET /api/profile` with `Authorization: Bearer invalid.jwt.token.here` | HTTP 401 Unauthorized (`{ error: 'Invalid or expired token' }`). | HTTP 401 Unauthorized; body: `{"error":"Invalid or expired token"}`. | **Pass** |
| **TC04** | **FR2** | Save Valid Multi-Platform Handles | `PUT /api/profile` with `github_username: "torvalds"`, `codeforces_handle: "tourist"`, `leetcode_username: "neal_wu"` | HTTP 200 OK; `profiles` row upserted and returned with all handles. | HTTP 200 OK; `profiles` row updated with `torvalds`, `tourist`, `neal_wu`; `updated_at` refreshed. | **Pass** |
| **TC05** | **FR2** | Save Profile with Partial/Empty Handles | `PUT /api/profile` with `github_username: "torvalds"`, `codeforces_handle: ""`, `leetcode_username: "neal_wu"` | HTTP 200 OK; empty string/null stored without validation crash or blocking other sources. | HTTP 200 OK; `codeforces_handle: ""` stored; GitHub & LeetCode remain intact. | **Pass** |
| **TC06** | **FR3** | Multi-Platform Synchronous Ingestion | `POST /api/stats/refresh` with Bearer JWT (handles: `torvalds`, `tourist`, `neal_wu`) | HTTP 200 OK; new snapshot rows inserted into `stats_snapshot` for all 3 sources. | HTTP 200 OK; `refreshed: [{source: "github", value: 13, ok: true}, {source: "codeforces", value: 3530, ok: true}, {source: "leetcode", value: 253, ok: true}]`. | **Pass** |
| **TC07** | **FR3, NFR1** | Platform Error Isolation (Single Invalid Handle) | `PUT /api/profile` with invalid GitHub `nonexistent_user_xyz_9988_never_exists` and valid CF/LC handles; trigger `POST /api/stats/refresh` | GitHub returns error object; Codeforces and LeetCode succeed and persist snapshots. | HTTP 200 OK; `githubError: "GitHub API 404"`, `cfSuccess: true (val: 3530)`, `lcSuccess: true (val: 253)`. Pipeline did not abort. | **Pass** |
| **TC08** | **FR4** | Code Health Score Dynamic Calculation | Directly executed exported `calculateCodeHealth(latestGithub, latestCodeforces, latestLeetcode)` from `frontend/lib/scoring.ts` with real snapshot objects (`GH repos: 12`, `CF rating: 3530`, `LC byDifficulty: Easy:50, Med:150, Hard:53`, `GH weeks: 24 active`) | `consistency: 100`, `diff: 100`, `repo: 48`, `contest: 100` $\rightarrow$ `totalHealth: 87`, `grade: "A"`. | Real exported module returned: `scores: { consistency: 100, problemDifficulty: 100, repoQuality: 48, contestActivity: 100 }`, `totalHealth: 87`, `grade: "A"`. Matches rendered values in `CodeHealthPanel` visual verification. | **Pass** |
| **TC09** | **FR5** | Unified Activity Timeline Sorting | Extracted `ghMeta.recentEvents` (commits, PRs) and `cfMeta.recentEvents` (accepted submissions) | Combined array is strictly sorted in reverse-chronological order by `timestamp`. | `totalEvents: 10`, `isDescendingSorted: true`, platforms: `["github", "codeforces"]`, top item: `"Pushed to torvalds/linux"`. | **Pass** |
| **TC10** | **FR6** | Avatar Upload & Storage Bucket Persistence | `POST /api/profile/avatar` with WebP base64 payload | Base64 decoded, stored in `avatars` bucket, returns public CDN URL. | HTTP 200 OK; `publicUrl: "https://<PROJECT_REF>.supabase.co/storage/v1/object/public/avatars/e36921a5-142c-4922-b5f2-b18c07f3833e-...webp"`. | **Pass** |
| **TC11** | **FR7** | Automated Scheduled Refresh (Valid Secret) | `POST /api/internal/refresh-all` with header `x-cron-secret: <CRON_SECRET>` | HTTP 200 OK; `refreshAllUsers()` triggers batch ingestion for all registered profiles. | HTTP 200 OK; `{"status":"ok","message":"Refresh completed"}`; all user profiles updated. | **Pass** |
| **TC12** | **FR7, NFR2** | Background Refresh Unauthorized Access | `POST /api/internal/refresh-all` with invalid header `x-cron-secret: wrong_secret_123` | HTTP 401 Unauthorized; batch processing blocked. | HTTP 401 Unauthorized; `{"error":"Invalid or missing cron secret"}`. | **Pass** |
| **TC13** | **FR7, NFR1** | Batch Job Fault Tolerance on Platform Outage | Independently executed: temporarily set broken handle (`github_username: "invalid_batch_nonexistent_user_9999_never_exists"`) on target user profile in DB; invoked `POST /api/internal/refresh-all` | Batch job catches GitHub 404, logs error, enforces 1000ms delay, successfully refreshes Codeforces and LeetCode, and continues across remaining users without crash. | HTTP 200 OK (`{"status":"ok","message":"Refresh completed"}`); backend logged `github:fail` for target user while `codeforces:ok` and `leetcode:ok` succeeded, and subsequent users completed. Original handle restored. | **Pass** |
| **TC14** | **NFR2** | Forged `user_id` in Protected Request Body | `PUT /api/profile` with body `{ user_id: "00000000-0000-0000-0000-000000000000", github_username: "torvalds" }` authenticated as user `e369...` | Server ignores body `user_id` and binds operation strictly to verified `req.user.id`. | HTTP 200 OK; returned row has `user_id: "e36921a5-142c-4922-b5f2-b18c07f3833e"`. Forged ID completely ignored. | **Pass** |
| **TC15** | **Regression (Bug 1)** | Trend % Indicator Dynamic Delta vs Static 0% | Directly executed exported `trendPercent(series)` from `frontend/lib/api.ts` with series: single snapshot `[100]`, increase `[100, 150]`, decrease `[100, 80]`, zero-prev `[0, 50]` | `trendPercent` returns `null` for $N<2$ snapshots (neutral badge) and calculated percentage delta for $N \ge 2$. Returns `null` when base is `0`. | Real exported module returned: `singleSnapshot: null`, `increase: 50` (+50%), `decrease: -20` (-20%), `zeroPrev: null`. Verified on live overview KPI cards. | **Pass** |
| **TC16** | **Regression (Bug 2)** | Growth Comparison Normalization at Disparate Scales | Directly executed exported `normalizeToRange(data)` from `frontend/lib/scoring.ts` with multi-scale arrays: GitHub `[18, 20]`, Codeforces `[709, 750]`, LeetCode `[133, 150]`, Flat `[50, 50]` | `normalizeToRange` scales each series independently to `[0, 100]`. Flat series maps to `100`. No series collapses to flat zero line. | Real exported module returned: `gh: [0, 100]`, `cf: [0, 100]`, `lc: [0, 100]`, `flat: [100, 100]`. Verified on rendered Stats page growth chart. | **Pass** |
| **TC17** | **Regression (Bug 3)** | Cross-Page Sync Timestamp Consistency | Inspect latest sync timestamp on Overview (`app/page.tsx`) vs Refresh page (`app/refresh/page.tsx`) | Both pages query `getStats()` from `public.stats_snapshot` as single source of truth; both format using `relativeTime()`. | Synchronized: both pages reflect identical DB timestamps (`github`, `codeforces`, `leetcode` timestamps from `public.stats_snapshot`). | **Pass** |
| **TC18** | **FR8, NFR2** | Public Shareable Profile Retrieval & Data Sanitization | `GET /api/public/torvalds` without authorization header | HTTP 200 OK; returns public developer card with Code Health and handles; zero sensitive fields (`user_id`, `email`) exposed. | HTTP 200 OK; body: `{ username: "torvalds", handles: {...}, stats: {...}, health: { total: 87, grade: "A" } }`. `user_id` and `email` are completely absent. Nonexistent username returns clean 404. | **Pass** |
| **TC19** | **FR9, NFR2** | Head-to-Head Compare Parallel Fetching & Validation | `GET /api/public/compare/torvalds/lassioverflow`, `GET /api/public/compare/torvalds/torvalds`, and `GET /api/public/compare/torvalds/nonexistent_user_99` | Parallel `Promise.all` lookup. Identical users return 400 Bad Request; nonexistent user returns 404 with missing user identity; valid pair returns sanitized comparison payload. | Valid pair: HTTP 200 OK (`{ user1: {...}, user2: {...} }`). Identical users: HTTP 400 Bad Request (`{"error":"Cannot compare a user with themselves"}`). Missing user: HTTP 404 Not Found (`{"error":"User 'nonexistent_user_99' not found"}`). | **Pass** |
| **TC20** | **FR10** | Weekly Digest Ingestion & Delta Calculation | `POST /api/internal/send-digests` with header `x-cron-secret: <CRON_SECRET>` | Computes week-over-week deltas for opted-in users; skips users with zero delta; dispatches via mxroute SMTP (`statmux@sayan.cyou`). | HTTP 200 OK; batch dispatched; identical snapshots logged as `[digest:skipped] reason: no_meaningful_change`; test email delivered to `sayan@sayan.cyou` with MessageId. | **Pass** |
| **TC21** | **FR10, NFR2** | Cryptographic One-Click Unsubscribe | `GET /api/digest/unsubscribe?uid=<USER_ID>&token=<HMAC_TOKEN>` vs invalid token | Valid signed HMAC token flips `subscribed` to `false` without requiring login and renders branded confirmation page; tampered token returns 400. | Valid token: HTTP 200 OK; rendered "Unsubscribed Successfully" and updated DB record. Invalid token: HTTP 400 Bad Request with "Invalid or Expired Link". | **Pass** |
